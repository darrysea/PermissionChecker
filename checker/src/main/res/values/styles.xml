<resources>

    <!-- Base application theme. -->
    <style name="PermissionCheckerTheme" parent="@android:style/Theme.Translucent.NoTitleBar">
        <!-- Customize your theme here. -->
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
